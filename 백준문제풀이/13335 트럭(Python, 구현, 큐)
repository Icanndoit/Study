n, brige_len, brige_weight = map(int, input().split())
#  트럭의 개수, 다리 길이, 다리 무게

data = list(map(int, input().split()))
queue = [[x, 0] for x in data]

# 다리위에 트럭이 올라가려면, 올라갈 경우 트럭들의 길이 합이 다리 길이보다 길면 안된다.
# 다리 위의 트럭들 수가 다리 길이보다 짧아도, 합한 중량이 다리의 무게보다 클 수 없다.
# 트럭이 다리 위에 올라가면, 시간 + 1이 된다. 다리 위에 트럭이 있었을 경우 먼저있던 트럭은 한칸 더 이동한다.
# 트럭이 다리 위를 완전히 빠져나가면, 큐에서 제거한다.
# 마지막 트럭이 다리를 완전히 통과하면 시간을 출력한 후 프로그램이 종료된다.

truck_len, truck_weight = 0, 0 # 다리 위 트럭의 개수, 다리 위 트럭 무게의 합
time = 0

while len(queue) != 0:
    time += 1

    # 다리 위 트럭들 한 칸씩 이동
    for i in range(truck_len):
        queue[i][1] += 1
    
    # 다리를 완전히 건넌 트럭 제거
    if truck_len > 0 and queue[0][1] > brige_len:
        truck_weight -= queue[0][0]
        truck_len -= 1
        del queue[0]

    # 새 트럭을 다리에 올릴 수 있는지 확인
    if truck_len < len(queue):
        if ((queue[truck_len][0] + truck_weight) <= brige_weight 
            and (truck_len + 1) <= brige_len):
            truck_weight += queue[truck_len][0]
            queue[truck_len][1] = 1
            truck_len += 1

print(time)
