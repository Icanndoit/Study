size, hap = int(input()), 0
lst1 = [*map(int, input().split())]
lst2 = [*map(int, input().split())]

while size > 0:
    hap += (max(lst1) * min(lst2))
    lst1.remove(max(lst1))
    lst2.remove(min(lst2))
    size -= 1
print(hap)
